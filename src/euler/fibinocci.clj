(ns euler.fibinocci)

;(defn one? [n] (= 1 n))
;
;(defn fib2 [x]
;    (if (or (zero? x) (one? x)) x
;      (+ (fib2 (- x 1)) (fib2 (- x 2)))))

;(prn (map (fn [x] (fib x)) (range 10)))

;(iterate (fn [x] (if (or (zero? x) (one? x)) x
;      (+ (fib (- x 1)) (fib (- x 2)))))
