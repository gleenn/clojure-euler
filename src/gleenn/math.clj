(ns gleenn/math)
;  (:require [clojure.strings :as strings])
;  (:use clojure.strings))

(defn square [x] (* x x))

;(defn add [x y?]
;  (if y?
;
;  )
;)
;
;(prn (map omg_shut_ur_face/square [1,2,3]))
